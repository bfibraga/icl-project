{
    let x = 10;
    let y = new(0);
    let z = new(y);
    let a = {
        let w = new(false);
        while val(w) {
            w -> ( (val(z) -> val(val(z)) + val(y) + 1) < x );
            println(val(w))
        };
        val(z) -> 1000000
    };
  println(x)
};;
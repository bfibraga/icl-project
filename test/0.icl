{
    let const = 10;
    let mut name = 'Braga';
    let x = new([
        mut id: 0,
        name: name
    ]);
    println(const),
    if val(x.id) != 0 {
        println('Displaying name'),
        name -> 'Bruno'
    } else {
        println('Error'),
        while val(x.id) < 5 {
            name -> val(name) + val(x.id),
            x.id -> val(x.id) + 1
        },
        val(x)
    }
};;

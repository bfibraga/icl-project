/*
    Sandbox Program
*/
{
    // Single comment line
    let value : int = 2;
    fn f(x:int, y:bool, z:bool) -> str{
        if x > 0 and y or z {
            'Sim'
        } else {
            'Nao'
        }
    };
    println(f(1, false, true)) //prints false
},,
{
    let MAX : int = 10;
    let mut x : int = MAX;
    let y : str = new('Hello world');
    let mut w = arr[MAX, x, y];
    let mut p : rec = [
        id: x,
        list: w,
        mut active: false
    ];
    val(w);
    x -> MAX + 1;
    y -> new('world Hello');
    val(p.list.get(1))
},,
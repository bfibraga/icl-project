/* Test 0 - Primitive Types */
{
    let mut x : int = 1;
    let mut y : bool = false;
    y -> true;
    {
        let mut y = new(val(x) + 1);
         x -> val(x) + val(val(y));
        val(x)
    } > 10 or val(y)
},,